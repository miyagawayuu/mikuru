# Mikuru Sample

Run this example using the repo's dev `vite` installation from the repository root:

```bash
npx vite --config examples/mikuru-sample/vite.config.ts
```

Then open http://localhost:5174 in your browser.

This example imports the local `src` runtime via an alias so you can iterate on the library and see changes immediately.
